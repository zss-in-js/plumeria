---
title: ServerCSS
description:  API reference for the The `ServerCSS` headless component
---

## Headless component

A headless component for facilitating server-side js for Next.js only.

<Callout title="It is safe to know">
  It will be null in non-development environments and will not be included in production environments.
</Callout>

```tsx title="layout.tsx"
import { ServerCSS } from '@plumeria/next';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html>
      <head>
        <ServerCSS /> // [!code ++]
      </head>
      <body>
        <main>{children}</main>
      </body>
    </html>
  );
}
```

<Cards>
  <Card title="See more about Installation" href="/docs/getting-started/installation" />
  <Card title="See more about @plumeria/next package" href="/docs/reference/next" />
</Cards>