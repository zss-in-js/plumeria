---
title: 'Installation'
description: "It is plumeria's installation, compilation, integration setup"
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from "fumadocs-ui/components/steps";

<Steps>
<Step>
## Runtime

<Tabs items={['npm', 'yarn', 'pnpm']}>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    npm i @plumeria/core
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    yarn add @plumeria/core
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    pnpm i @plumeria/core
    ```
  </Tab>
</Tabs>
If you are using Next.js, you need to configure next-plugin, and if you are using Vite, you need to configure vite-plugin.
You can install the compiler in stages.

</Step>
<Step>
## Compiler

<Tabs items={['npm', 'pnpm', 'yarn']}>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    npm i -D @plumeria/compiler
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    pnpm i -D @plumeria/compiler
    ```
  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    yarn add -D @plumeria/compiler
    ```
  </Tab>
</Tabs>
- `npx css`  
use swc compile without type-check.
- argument `--type-check`  
The  will include compilation type-check.

- arugment `--view`  
Display the CSS log that is output during compilation.

- argument `--paths`  
Display the compile target paths list is output during compilation.

## Integration with build

Like Vite and Next, there is no need to compile at the same time as dev.
<Tabs items={["Next.js", "Vite"]}>
<Tab>
```json title="package.json"
  "scripts": {
    "dev": "css && next dev",
    "build": "css --type-check && next build",
  }
```
</Tab>
<Tab>
```json title="package.json"
  "scripts": {
    "dev": "vite",
    "build": "css --type-check && tsc -b && vite build", 
  },
```
</Tab>
</Tabs>

## Static Stylesheet

- Applying a Style Sheet: import `@plumeria/core/stylesheet.css` in your application entry point.
```ts
import "@plumeria/core/stylesheet.css";
```



</Step>
<Step>
## Integration

Plumeira can be integrated with Vite and Next.js.

<Tabs items={["Next.js", "Vite"]}>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    npm i -D @plumeria/next-plugin
    ```

  </Tab>
  {/* prettier-ignore */}
  <Tab>
    ```sh title="Terminal"
    npm i -D @plumeria/vite-plugin
    ```
  </Tab>
</Tabs>
</Step>
<Step>
## Next.js

Plugin is compiling Server Components and Inject virtual CSS imports for HMR.  
The client component is injected by the runtime from @plumeria/core.
```ts title="next.config.ts"
import type { NextConfig } from "next";
import { withPlumeria } from "@plumeria/next-plugin";

const nextConfig: NextConfig = withPlumeria({
  /* config options here */
});

export default nextConfig;
```

</Step>
<Step>
## Vite
Vite is a client-based bundler, so it externalizes fs with plugins.
```ts title="vite.config.ts"
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';
import plumeria from '@plumeria/vite';

export default defineConfig({
  plugins: [react(), plumeria()],  // [!code ++]
});

```
</Step>
<Cards>
  <Card title="See more about @plumeria/vite-plugin package" href="/docs/reference/vite-plugin" />
  <Card title="See more about @plumeria/next-plugin package" href="/docs/reference/next-plugin" />
</Cards>
</Steps>