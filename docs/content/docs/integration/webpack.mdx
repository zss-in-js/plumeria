---
title: 'Webpack'
description:  'Learn more about @plumeria/webpack-plugin.'
---
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

[`@plumeria/webpack-plugin`](https://www.npmjs.com/package/@plumeria/webpack-plugin) is the Webpack plugin for Plumeria.  
It compiles styles into CSS via AST parsing, with HMR support.

## Installation
<Tabs items={['npm',  'yarn', 'pnpm']}>
<Tab>
```sh title="Terminal"
npm i --save-dev @plumeria/webpack-plugin
```
</Tab>
<Tab>
```sh title="Terminal"
yarn add -D @plumeria/webpack-plugin
```
</Tab>
<Tab>
```sh title="Terminal"
pnpm i --save-dev @plumeria/webpack-plugin
```
</Tab>
</Tabs>

## API
Example for: [`Github next-plugin ↗`](https://github.com/zss-in-js/plumeria/blob/main/packages/next-plugin/src/index.ts)

```ts title="next.config.ts"
config.module?.rules?.unshift({
  enforce: 'pre',
  test: /\.(tsx|ts)$/,
  exclude: [/node_modules/, /\.next/, /\.git/],
  use: require.resolve('@plumeria/webpack-plugin'),
});
```
