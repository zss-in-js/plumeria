---
title: 'keyframes'
description: 'API reference for the keyframes method'
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

```ts
export type keyframes = (rule: Keyframes) => string;
export const keyframes: keyframes;
```

`keyframes()` generates a unique animation variable name (an 8-character hash) that can be used in styles.

## Example

<Tabs items={["ts", "css"]}>
  <Tab>
    ```ts title="TypeScript"
    import * as style from '@plumeria/core'

    const fadeIn = style.keyframes({
      from: {
        opacity: 0,
      },
      to: {
        opacity: 1,
      },
    });

    const styles = style.create({
      box: {
        animationName: fadeIn,
        animationDuration: '1s',
        animationTimingFunction: 'ease-in-out',
      },
    });
    ```
  </Tab>
  <Tab>
    ```css title="CSS"
    @keyframes kf-x34h9adh {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    .xhc714s1:not(#\#) { animation-name: kf-x34h9adh; }
    .xdm4omap:not(#\#) { animation-duration: 1s; }
    .xvct32ng:not(#\#) { animation-timing-function: ease-in-out; }
    ```
  </Tab>
</Tabs>

## Usage

You can also trigger animations on hover or using other pseudo selectors:

```ts
const styles = style.create({
  card: {
    transition: 'transform 0.3s ease',
    ':hover': {
      animationName: fadeIn,
      animationDuration: '0.5s',
    },
  },
});
