---
title: 'variants'
description: 'API reference for the variants'
---

```ts
export type variants = <T extends Variants>(rule: T) => (props: { [K in keyof T]?: keyof T[K] }) => CSSProperties;
export const variants: variants;
```
`variants` return functions that can accept arguments dynamically.

## Example

```tsx
import * as style from "@plumeria/core";

const getButtonStyle = style.variants({
  variant: {
    gradient: styles.gradient,
    metallic: styles.metallic,
  },
  size: {
    small: styles.small,
    medium: styles.medium,
    large: styles.large,
  },
});

interface Props {
  children: ReactNode;
  variant: 'gradient' | 'metallic';
  href: string;
}

export const ButtonLink = ({ children, variant, href }: Props) => {
  return (
    <Link href={href} className={style.props(getButtonStyle({ variant, size: 'medium' }))}>
      {children}
    </Link>
  );
};
// call Components
<ButtonLink href={"/docs"} variant="metallic">{...}</ButtonLink>
```

<Callout title="Good to know">
    After being expanded to className, it is safely deleted.  
    If it is statically determined first, only styles for that key are generated.
</Callout>