---
title: 'global'
description: 'Define global styles including elements, classes, pseudo-classes, and media queries.'
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

The `global` method defines styles that apply globally to the entire document.  
It supports HTML elements, class names, and pseudo-classes or media queries — all **with full type support** and **compile-time extraction**.

<Callout title="Semmantic API">
Keep global styles to a minimum to reduce bundle size.
</Callout>


## 🌍 Example

<Tabs items={['ts', 'css']}>
  <Tab>

```ts title="TypeScript"
import { css } from '@plumeria/core'

css.global({
  body: {
    margin: '0 auto',
    color: 'white',
    background: '#303030',
  },
  h2: {
    fontSize: '32px',
    fontWeight: '500',
    color: 'white',
  },
  '.highlighted': {
    fontSize: '24px',
    fontWeight: '450',
    color: 'aqua',
  },
});
```
</Tab>
<Tab>
```css title="css"
body {
  margin: 0 auto;
  color: white;
  background: #303030;
}
h2 {
  font-size: 32px;
  font-weight: 500;
  color: white;
}
.highlighted {
  font-size: 24px;
  font-weight: 450;
  color: aqua;
}
```
</Tab>
</Tabs>
