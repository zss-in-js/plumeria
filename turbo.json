{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "!dist/**/*.d.ts", ".next/**", "!.next/cache/**"]
    },
    "test": {
      "cache": true
    }
  }
}
