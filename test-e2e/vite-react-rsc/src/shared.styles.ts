import * as css from '@plumeria/core';

export const shared = css.create({
  panel: {
    padding: '8px',
    borderColor: 'rgb(10, 20, 30)',
    borderStyle: 'solid',
    borderWidth: '2px',
  },
});
