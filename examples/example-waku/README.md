# example-waku

Plumeria on [Waku](https://waku.gg/), based on the `01_basic` starter template.

```sh
pnpm dev
pnpm build && pnpm start
```

`src/pages` and `src/components/{header,footer}.tsx` are server components and
`src/components/counter.tsx` is a client component; both use `css.create`.
