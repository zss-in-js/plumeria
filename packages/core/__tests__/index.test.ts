import { css, x } from '../src/index';

describe('css rx', () => {
  it('should be defined', () => {
    expect(css).toBeDefined();
  });
  it('should be defined', () => {
    expect(x).toBeDefined();
  });
});
